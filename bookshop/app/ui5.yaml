specVersion: '2.6'
metadata:
  name: bookshop
type: application
resources:
  configuration:
    paths:
      webapp: finished-webapp
server:
  customMiddleware:
    - name: fiori-tools-proxy
      afterMiddleware: compression
      configuration:
        backend:
          - path: /browse
            url: https://services.odata.org/V2/Northwind/Northwind.svc/